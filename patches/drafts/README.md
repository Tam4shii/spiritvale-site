# patches/drafts/

Raw scaffolding files generated during patch processing. Contains `raw_body` from Steam
announcements and empty structured arrays — superseded by the versioned `patches/v*.json` files.

Not served by the site or validated by CI. Kept for provenance/archaeology only.
